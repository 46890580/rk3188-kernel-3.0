#
# temperature sensor drivers configuration
#

menuconfig TEMPERATURE_DEVICE
	bool "temperature sensor device support"
	default n

if TEMPERATURE_DEVICE
 config TMP_MS5607
 bool "temperature sensor ms5607"
 default n
 
 config TMP_TC74
 bool "temperature sensor tc74"
 default n

endif
